# Spec Registry — overlap & dependency index

> One row per feature/FRS. `/crosscheck` reads this to answer three questions about a new
> design: **who else touches this?** (overlap), **what must exist first / what breaks
> downstream?** (dependencies), and **which siblings share this concept?** (harmonization).
>
> The overlap signal is mechanical: if two rows share an **entity**, a **route/page**, or a
> **shared concept**, they overlap and should be coordinated. Keep entity/route names exact
> (match `verified-data-models.md` and `admin-ia-inventory.md`) so matching works.
>
> **Seeded from known work — partial on purpose.** Columns marked `?` need confirmation.
> Add a row whenever `/specify` produces a new FRS.

Legend — **Status:** built / specced / draft / idea. **Deps:** upstream (must exist first) →,
downstream (affected by this) ←. **Docs:** `—` specced only · `pending` Epic created/shipping,
Feature Doc owed · `N/A` Docs N/A ticked · `<pageId>` published Confluence manual page
(see `references/docs-spine.md`).

| Feature | Entities touched | Routes / pages | Shared concepts | Upstream deps (→) | Downstream deps (←) | Jira | Docs | Status |
|---|---|---|---|---|---|---|---|---|
| Application/Common Properties | ApplicationProperty (61) | `/MasterListsPage/commonproperties` | Feature Flags, booleans | — | Feature Flags tab; many features read props | ? | — | built/redesign specced |
| Site Information | SiteInformation (29) | `/MasterListsPage/SiteInformationMenu` | multi-locale banner, PasswordInput | — | Site Branding | ? | — | specced |
| Validation Configuration | validation charset (4) | `/MasterListsPage` (General Config → Validation) | charset rules | — | Patient/Order entry validation | ? | — | specced |
| WorkPlan Configuration | workplan booleans (3) | `/MasterListsPage/WorkPlanConfigurationMenu` | workplan columns | — | Workplan operational views | ? | — | specced |
| Order & Patient Entry config | OrderEntry (14) + PatientEntry (8) props | `/MasterListsPage/SampleEntryConfigurationMenu` | conditional field toggles | Validation Config (charset) | Order entry wizard | ? | — | specced |
| Menu Configuration | menu nodes (5 scopes) | `/MasterListsPage/*MenuManagement` (sub-routes) | Side Nav Active toggle, Feature Flags | — | every SideNav-placed feature | OGC-556 | — | specced |
| Test Notification | notification config (4 channels) | `/MasterListsPage/testNotificationConfigMenu` | template fallback, substitution vars | — | Test Catalog Alerts (delivery) | ? | — | built/specced |
| Test Catalog Alerts | per-test alert rules | Test Catalog → Alerts tab | **Critical Acknowledgment**, Test Notification delivery | Test Notification system | Critical-result ack feed | ? | — | specced |
| Analyzer Types & Mapping | Analyzer, AnalyzerType/profile (NEW), Test + result options + LOINC, Lab unit, pending/unmapped queue, QC codes | `/analyzers/types`, `/analyzers/{id}/mappings`, `/analyzers/types/{typeId}`, inline setup in `/analyzers` | value/select-list maps, **Deactivate**, LOINC 1:1 match, **search ComboBox**, **Alerts** + critical-ack, `ANALYZER_*` audit, learn-from-traffic | Test catalog (dictionary/LOINC); pending-queue (extend); **Alerts ack model (critical-ack, not built)** | Analyzer Maintenance (shared `/analyzers/{id}` namespace); Mgmt Dashboard OGC-897; Home OGC-896; Quality Control | ? | — | specced |
| Analyzer Maintenance & Service | Analyzer, **EquipmentMaintenanceLog (NEW)**, StorageRoom, Dictionary (event types), Alert | `/analyzers/maintenance`, `/analyzers/{id}/configuration` (+ old admin redirect) | **Alerts** (`EQUIPMENT_MAINTENANCE_DUE`), Dictionary-extensible types, Deactivate, verifiedBy (ISO 15189) | Analyzers IA baseline (D-027); Alert entity | Mgmt Dashboard OGC-897 (equipment cards); Home OGC-896 (Lab Snapshot) | ? | — | specced |
| QA Dashboard / TAT | TAT metrics (Average only v1) | QA menu | **TAT threshold model** | — | **Home-page Attention/TAT (must reuse)** | ? | — | specced (v1) |
| Referral redesign | Referral, ReferralStatus enum, SampleShipment | `/SampleShipment/reference-lab-results` | **ReferralStatus** in-transit signal | FHIR referral | shipment tracking | OGC-796 | pending (OGC-1113) | specced |
| EQA V2 | EQA program/participant/result | `/EQADistribution`, `/EQAManagement` | EQA badges | **EQA V2 controller (NOT built)** | EQA participant oversight (separate lane) | ? | — | specced, blocked |
| Report Print Queue | report jobs/queue | `/Report`, JSP `/ReportPrint` | print presets | — | report delivery | OGC-1031 (+1032–1043), anchor OGC-431 | — | specced |
| Label Presets | LabelPreset (configurable) | `/labelPresets`, Test Catalog Labels section | per-sample presets, allow_override | — | Test Catalog Labels (FR-66/67), Order Entry Labels | OGC-285 (spec v2.5) / OGC-284 | — | built + spec evolving |
| Critical Result Acknowledgment | result ack flag | Alerts dashboard, result views | **Critical Acknowledgment**, alerts feed | — | Test Catalog Alerts, home Attention feed | ? | — | idea/global TODO |
| Reagent ↔ Test linkage | Reagent, Test | Test Catalog → Reagents tab | inventory | **Test↔Reagent linkage (NOT built)** | reagent inventory | ? | — | blocked |
| Inventory redesign | InventoryItem, InventoryLot, InventoryTransaction, InventoryUsage (+ type tag, UPC, reorder horizon, per-item lead time, consumption mean+SD) | `/inventory`, `/inventory/receive`, `/inventory/reports` | **sample Storage model (OGC-657)**, Test↔Reagent link (auto-consume), **Alerts** (ack-to-quiet, D-042), OGC-436 forecast | sample Storage model — OGC-657 **in PR #3840 (open, occupant_type on sample_storage_assignment/movement, shared LocationPickerModal)**; Test↔Reagent link (built) | multi-lab oversight = external BI via FHIR (D-041), not in-app | OGC-657 (PR #3840); OGC-658 closed superseded (D-038); OGC-436 | — | FRS v1.3 draft |
| Env/Vector order entry | Sample, Order (4-step wizard), aliquot LABNO.X-Y | order entry wizard | Domain (ENV/VECTOR), NCE, validator-final | FHIR referral (vector pools) | validation, deconvolution | OGC-527 | — | specced |
| Test Catalog Completion v2 | Test, TestResultComponent, test_result options, SAMPLETYPE_TEST, result_limits, variant-link (NEW datum), test_label_preset_link | `/admin/TestCatalogList`, `/MasterListsPage/TestCatalogEditor/*` | Domain enum, LOINC routing, Deactivate, completeness gate, catalog health | delivered OGC-949 editor; Label Preset Mgmt (built); OGC-285 Labels contract | order entry (grouped list), e-order intake (variant link), sample-type + panel + lab-unit specs | OGC-949 family | — | specced (passed /analyze 2026-07-14) |
| Sample Type Management v2.1 | TypeOfSample, SAMPLETYPE_TEST (read), terminology store (NEW) | `/admin/TestCatalogList?entity=sampletypes` | Domain enum (migration), read-only Associated Tests, WHONET | shell contexts; domain migration | order entry sample-type menu | OGC-296, OGC-538 | — | specced + mockup |
| Panel Management v2.2 | Panel, PanelItem, SAMPLETYPE_PANEL (sync), terminology store (NEW) | `/admin/TestCatalogList?entity=panels` | Domain enum (migration), derived sample types, LOINC routing | panel.domain migration; OGC-949 shell | order entry panel list, FHIR intake | OGC-224, OGC-1140 | — | specced + mockup |
| Lab Units Management v2.0 | TestSection (TEST_SECTION) | `/admin/TestCatalogList?entity=labunits` | Domain enum (migration; OGC-361 NOT on develop), read-only Associated Tests | shell contexts; TEST_SECTION domain migration (reconcile OGC-361) | workplans, result-entry grouping | OGC-189, OGC-290, OGC-361 | — | specced + mockup |
| Analyzer Results Lab Unit Access | Analyzer (`test_unit_ids`), AnalyzerTestMapping, Test → TestSection, **UserLabUnitRoles / LabUnitRoleMap**, SystemModuleUrl | `/AnalyzerResults?id=<analyzerId>` (+ legacy `?type=`), `/rest/AnalyzerResults`, generated **Results ▸ Analyzer** menu entries, `/analyzers` list column; gallery `analyzer-integration` | **lab-unit-scoped Results rights** (`getUserTestSections`, `AllLabUnits` sentinel, `REQUIRE_LAB_UNIT_AT_LOGIN`), Access Denied state, menu filtering, backfill migration, labels-not-counts | **OGC-1057 (lab-unit picker on analyzer form — blocks)**; OGC-1151 (shared menu-filter mechanism) | **OGC-1137** (inherits the rule); OGC-288 (hosts hidden-rows notice); **Runs (OGC-1200): generated Results ▸ Analyzer menu entries retire, gate consumed by the Runs chip and `/Results?run=`; `/AnalyzerResults?id=` redirects)**; RBAC revamp | OGC-1178 | pending | specced + mockup |
| Vector host index | host index | ? | Domain VECTOR | Env/Vector order entry | — | OGC-527 (story pending) | — | idea |
| M-00 Microbiology Module (parent) | micro_case, micro_isolate, micro_ast_run, result (multi-reading) | `Microbiology` top-level: Worklist, Case Search, Antibiogram | workflow_type (BACTERIOLOGY / MYCOBACTERIOLOGY_TB), no per-case ownership (shared state-driven queue), reuse-first | — | every M-* spec | OGC-782 | — | specced; **built on the OGC-782 stacked branch, NOT on `develop`** |
| M-01 AMR Reference Data | Organism Master, Antibiotic Master, AST Panels, Culture Protocols (as Methods), **patient_origin (NEW)**, department `whonet_code` | `Admin → Microbiology Reference Data → *` | WHONET codes, selective/cascade reporting, origin vocabulary | existing Method + Department vocabularies | M-09, M-13, M-14, M-16 | OGC-782 family | — | specced |
| M-02 Breakpoint Catalog | breakpoint tables (CLSI / EUCAST / WHO-TB), version snapshots | `Admin → Microbiology Reference Data → Breakpoints` (?) | **breakpoint standard + version snapshot** (carried on every AST result) | M-01 masters; AMR-for-R extraction (legal clearance pending) | M-05, M-13, M-14, M-15, M-16 | OGC-916 (WHO-TB) | — | specced |
| M-04 Case Workbench Core | micro_case (`sample_item_id` + `workflow_type` unique), micro_isolate, micro_ast_run; ward / department_code / patient_origin / is_screening_culture | `/rest/microbiology/cases/*` (confirmed live on branch) | case lifecycle, amendment versioning, incubation clock (recompute-not-reset), inline-not-modal | M-03 trigger resolver, M-01, M-02 | M-05, M-07, M-09, M-13, M-15, **M-16** | OGC-782, OGC-926 | — | built on branch; **2 High UAT findings open (workflow 500; amended results indistinguishable)** |
| M-05 AST Entry & Interpretation | micro_ast_run, result, override audit | Case workbench inline AST panel | S/I/R interpretation, matched-breakpoint level, override + revert, analyzer RESULTS_IN | M-02, M-04 | M-13, M-15, M-16 | OGC-782 | — | built on branch |
| M-07 Micro Worklist | micro_case (state-driven) | `Microbiology → Worklist` (Cultures / AST grain toggle) | shared queue, no ownership, "AST results in — review" flag | M-04 | — | OGC-782 | — | specced |
| M-09 WHONET Export | **whonet_export_run (NEW)**, code mappings (organism/antibiotic/specimen/origin/patient-type/department/breakpoint/phenotype) | `/reports/whonet-export` + mapping admin | **first-isolate de-duplication — OWNER (D-049)**, WHONET code mapping, used-set readiness, `GPS_LATITUDE`/`GPS_LONGITUDE` export columns (built) | M-01, M-04, M-05 | **M-13 and M-16 both invoke the dedup**; M-15 | — | — | specced (Phase 1B); **dedup routine NOT built** |
| M-11 Critical Result Acknowledgment (micro) | result ack flag | Alerts dashboard, case views | **Critical Acknowledgment**, alerts feed, ack-to-quiet (D-042) | — | M-16 attention feed | — | — | idea / **not built** |
| M-13 Antibiogram | **antibiogram_run (NEW, non-clinical)**; reads micro_case / micro_isolate / micro_ast_run / result | `/reports/antibiogram`, `/reports/antibiogram/history` | **M-09 dedup (invokes, D-049)**, CLSI M39 n<30 suppress/flag, selective-cascade reporting, Jasper export | **M-09 dedup (not built)**, M-01 selective reporting, M-14 (TB variant) | — | — | — | specced |
| M-14 Mycobacteriology / TB | micro_case `workflow_type=MYCOBACTERIOLOGY_TB`, DST runs (`interpretation_method = CRITICAL_CONCENTRATION`), molecular flags | Worklist / Case surfaces with TB grain | WHO critical concentrations (R/S, not clinical S/I/R), MDR / pre-XDR / XDR derivation | M-02 WHO-TB family, M-04 | M-13 TB variant, M-15, M-16 | OGC-916 | — | specced |
| M-15 GLASS Surveillance via Consolidated FHIR | **surveillance_submission_run (NEW)**, `fhir_uuid` on micro_case / micro_isolate / micro_ast_run | `/admin/surveillance/fhir`, `/reports/surveillance-submission` (+ `/history`) | consolidated-FHIR push, **central aggregation OUTSIDE OpenELIS (D-041, D-050)**, AMR IG profiles, GLASS minimum dataset | M-04, M-05, **M-09 dedup**, M-02, M-14; FhirTransformService / FhirPersistanceService (built) | glass-amr-dashboard-indicators (OGC-918 / OGC-794); **M-16 priority-export flag** | OGC-918, OGC-794 | — | specced (Phase 2+) |
| M-16 Cluster Detection & Outbreak Signals | **cluster_detection_run, cluster_signal, cluster_signal_isolate, cluster_investigation, cluster_watchlist_entry, cluster_detection_config (all NEW, non-clinical)**; reads micro_case (ward/department/patient_origin/is_screening_culture), micro_isolate, micro_ast_run, **Person.gpsLatitude/gpsLongitude**, Sample GPS, VectorSamplingSite, Test | **`Surveillance` (NEW top-level group)** → `/surveillance/clusters`, `/surveillance/clusters/history`; `/MasterListsPage/clusterDetection` | **M-09 dedup (invokes, D-049)**, Alerts + ack-to-quiet (D-042), **Test Notification delivery (reused; recipient list is NEW)**, Domain enum extended to scan populations (D-004), Deactivate-not-Delete (D-002), detection-in-app vs analytics-external (D-050), alert budget (D-051) | **OGC-782 micro module (NOT on develop)**; **M-09 dedup (not built)**; M-11 alerts model (not built); OGC-896 attention feed; priority-export flag on M-09/M-15; `PATIENT_GPS_CAPTURE_ENABLED` default flip | M-15 (priority export), OGC-896 home, OGC-897 mgmt dashboard, OGC-918/794 publication spec | — | — | specced 2026-09-04; `/analyze` passed (2 CRITICALs found + fixed) |
| Runs (shared unit of QC, reagent lot, result review) | **Run (new)**, run-linked control record (new), Result → run ref (new), Analysis, Result (`component_id`), Test, TestSection, Analyzer, ReagentLot, ReagentConsumptionEvent, `QCSample`/`RunQCStatus`/`ImportedResult`, `qc_control_lot`, `test_qc_target` (+2 policy attrs), Workplan batch, NonConformity, Alerts | `/Results` (Runs + Held chips, Waiting/Held/Batched tags, Batch these), `/Results?run=<id>` (run header), `/AnalyzerResults?id=` → redirect, Results ▸ Analyzer menu retired, Workplan batch flow, Test Catalog → QC Targets policy block | **per-test QC verdict**, QC-fail signal (scope = run + covered test), reagent-lot provenance, run states Open/Held/Complete/Superseded, exception queue, critical ack, correction workflow, presence, D-043 lab-unit gate | run record storage (OGC-1147 D1; **not `QcRun`** per OGC-1054), source-typed controls (OGC-1147), `test_qc_target` (specced), pending-value lookup by accession (OGC-1137), per-row save + presence (OGC-1020), `RESULT.component_id` (OGC-1124 done), Workplan batch service (OGC-427) | OGC-288, OGC-1137, OGC-1025, OGC-1147, OGC-427, OGC-1178, QC Targets spec, Validation (hold reason), QC dashboard (run list) | **OGC-1200** (Epic, Samuel) | pending | specced + mockup + preview (`upload/runs-*`, gallery registration pending) |
| Results Entry (multicomponent, v4 lineage) | Analysis, Result (`component_id`), Method, Analyzer, ReagentLot, NCE, Referral, Sample storage | `/Results` (Workplan → Results) | edit-state machine, Method/Analyzer split, work zone / reference zone, contrast tags, critical ack, per-row save + presence (FR-O), cross-domain via Lab Unit | `RESULT.component_id` (OGC-1124 done); section D lot/control scope → **Runs** | Validation page (inherits split + multi-component), Analyzer Import v2 (FR-G1 reuse), Runs (row tags, FR-D7) | OGC-811 (Done), R-slices OGC-1020–1026 (OGC-1025 In Progress, Samuel) | ? | built (v3 unified worklist); multicomponent + R6 in progress |
| Analyzer Results Import v2 (QC-first review) | `QCSample`, `RunQCStatus`, `ImportedResult`, `RunSettings`, ReagentLot, Analysis, Result (`component_id`), `analyzer_test_map` | was `Analyzers → [analyzer] → Import`; **superseded IA → `/Results?run=<id>`** (Runs) | QC-first gate (now per test), exception queue (unmatched/unordered/unmapped), Keep/Replace/Ignore, Retest/Reject/Accept-despite (+NCE), accession-keyed matching, view raw, G2–G13 | OGC-1124/1129 component linkage (done), Analyzer Types & Mapping (OGC-1136) | Runs (owns Overview/IA, G1, FR-A2 scope) | OGC-288 (re-scoped 2026-09-09; draft PR #2584 closed unmerged) | — | specced (v2 FRS + preview in gallery); build not started |
| Batch Workplan reagent QC | Workplan batch (→ **run**, source WORKPLAN), ReagentLot, reagent-lot control (→ run control), `QcRun` (specced only, not in code) | Workplan pages; `/Results` Batch these (Runs FR-B4) | reagent-lot QC, run control, Pass/Fail vocabulary | Runs (batch is a run) | Runs, Manual/RDT QC persistence (D1 sharing) | OGC-427 (In Progress, Samuel) | — | specced; in progress |
| Analyzer Manual QC Recording (instrument-level) | Analyzer (+qcFrequencyType/Hours/qcRequired, new), `QcRun` (specced; **not target architecture per OGC-1054**) with `analyzerId`, SystemUser | per-analyzer import page → Manual QC Status panel (now one line in Runs run settings); `/analyzers` list overflow → Record Manual QC | instrument periodic QC (DAILY / PER_SHIFT / CUSTOM_HOURS), non-blocking (BR-AQC-005), `label.analyzerQc.*` Pass/Fail vocabulary reused by OGC-1147 | none new | Runs (surfaces status line), OGC-1147 (vocabulary) | OGC-428 (Selected for Development, Piotr; rewritten to minimal style 2026-09-09) | — | specced (FRS + JSX in gallery) |
| Manual & RDT QC persistence | source-typed QC result (ASTM/MANUAL/RDT), `qc_result`/`qc_control_lot` (Westgard), run-linked control (Runs), QC target snapshot, `qc_rule_violation` | QC dashboard Source filter, Manual QC leaf (OGC-692), Results Entry FR-D3/D4 capture (consumer) | QC-fail signal (**scope = run + covered test**), qualitative outcome (VALID/INVALID; RDT stays QC, not a test component), D1–D5 build-time decisions | Westgard Phase 2 stores (OGC-682), QC Targets (`test_qc_target`), **Runs record** (D1 must satisfy; not `QcRun`) | OGC-1025 (unblocks), Validation V1 signal, Runs, QC dashboard | OGC-1147 (In Progress, Samuel) | — | specced; in progress |
| Test Catalog — QC Targets & LOD/LOQ | **`test_qc_target` (new)**, `qc_control_lot` (link only), `test_result_component` (+`lod`,`loq`), **+ policy attrs: control required per run, on-missing warn/hold (Runs FR-H1)** | Test Catalog unified editor → QC Targets leaf (after Ranges); Sample & Results per-component LOD/LOQ | prefill precedence (lot override → level default → blank), snapshot-at-capture (FR-D1 → run control), deactivate/reactivate | OGC-949 editor scaffold (M2), `qc_control_lot` for overrides | OGC-1147 (consumes targets), Results Entry FR-D4 (prefill), Runs (policy + FR-C2 verdicts), OGC-775 (distinct: result uncertainty) | none yet (rides OGC-949 / OGC-1147) | — | specced (FRS + preview) |
| Westgard Phase 2 / statistical QC | `qc_result`, `qc_control_lot`, `qc_rule_violation`, corrective actions | QA menu → Statistical QC pillar (QC Dashboard, QC Alerts, QC Lot Management) | Levey-Jennings, Westgard rules, control-lot lifecycle, violation → corrective-action workflow | ASTM-fed analyzer QC; OGC-1147 extends to MANUAL | OGC-1147, QC Targets (lot overrides link here), Runs (out of scope: consumes only) | OGC-682 (umbrella), OGC-685 (Phase C), OGC-706 | ? | built (Phase A/B), Phase C in progress |
| Order Entry v4 (state lifecycle, save model, Requester restoration) | Sample (`labNo`), `sampleOrderItems` (`requestDate`, **`requestTime` NEW**, `referringSiteId`, `referringSiteDepartmentId`), Patient (`patientPK`, `patientUpdateStatus`), Organization (type 5 site / type 11 dept) | existing per-domain wizard paths (no new route/SideNav/breadcrumb) | lab-number reservation (D-057), explicit save model (D-056), dirty-state tracking, Requester field set, order date vs Required By | date-locale serialization fix (OGC-1146 P / gate G8) **must land first**; Order & Patient Entry config (required-field toggles); Sample Acceptance Checklist (S-09/OGC-580); lab-unit→domain assignment; OGC-391/392 scoping | Env/Vector order entry (OGC-527, shares the wizard + action bar); OGC-1051 (same dirty-state component); remaining OGC-1146 findings | OGC-1146 (findings AC/AD/AE/AF + annotations on L, V) | — | FRS v4 draft, `/crosscheck` run 2026-09-10 |

## High-overlap hotspots (watch these)
- **Alerts page + AlertType/acknowledgment model** — now a four-way shared sink: Critical
  Result Ack, Test Catalog Alerts, Analyzer Types & Mapping (unmapped codes/results), and
  Analyzer Maintenance (`EQUIPMENT_MAINTENANCE_DUE`), plus the home Attention feed. Design the
  AlertType taxonomy + ack model **once**; note the critical-ack direction isn't built yet.
- **Analyzers module IA + per-analyzer route namespace** — Analyzer Types & Mapping and
  Analyzer Maintenance both extend `/analyzers/{id}/<subpage>` and the Analyzers SideNav group,
  written independently. Governed by D-027; both specs must converge on one route map before
  `/breakdown`.
- **Critical Acknowledgment** — touched by Test Catalog Alerts, Critical Result Ack, the home
  Attention feed, and analyzer unmapped-code alerting. Design once, reference everywhere.
- **TAT threshold model** — QA Dashboard owns it; home-page TAT must reuse (D-015).
- **Feature Flags / Menu Config** — cut across Application Properties, Menu Configuration,
  and every SideNav-placed feature.
- **`/MasterListsPage/*`** — the admin shell; many config specs share it, so route/editorKey
  collisions and shared SideNav placement need coordination.
- **Domain enum** — every catalog/order feature; never BOTH (D-004).
- **First-isolate de-duplication** — M-09 owns the only implementation; M-13 and M-16 invoke it.
  Three consumers of one isolate set, so a second denominator anywhere is a CRITICAL finding (D-049).
- **Patient / sample GPS** — `Person.gpsLatitude`/`gpsLongitude` (patient registration, OGC-650,
  `PATIENT_GPS_CAPTURE_ENABLED`), `Sample`/`SampleItem` GPS (order entry, `gpsCoordinatesEnabled`),
  and `VectorSamplingSite` coordinates are **three separate location models**. M-16 reads all three;
  there is **no environmental sampling-site entity** (vector only). Don't assume one.
- **Detection vs analytics boundary** — in-app operational detection (M-16) vs external BI roll-up
  (M-15, OGC-918/794). D-050; extends D-041.

---

## Maintenance
`/specify` adds a row as a closing step (entities, routes, shared concepts, deps). The
monthly consolidation task reconciles the registry against new FRSs and fills `?` cells.
Confirm `built`/`blocked` status against `current-state-gotchas.md`.
